{
  "_readme": [
    "化石回归的本地配置模板。复制成 tests/fossils.local.json 并填入你本机**已关闭**的会话 id。",
    "为什么要本地化：真实会话 id 属用户数据，写进仓库会随公开分发一起泄露；",
    "本文件（.example）不含任何真实 id，可安全进仓库。",
    "stable=true  ⇒ 该会话已关闭、内容不再变 ⇒ 允许精确断言（cut/total）。",
    "stable=false ⇒ 会被用来做**只读**回归（务必只用 dry-run），只断言不变量（verify OK / 零残留）。",
    "⚠️ 绝不要把活跃会话标成 stable —— 它每轮都在长，写进基线必然每跑必红。"
  ],
  "fossils": [
    {
      "id": "<已关闭的会话 id>",
      "stable": true,
      "why": "这句是给未来的自己看的：这个样本钉的是什么行为"
    },
    {
      "id": "<另一个会话 id>",
      "stable": false,
      "why": "不变量断言：只要 verify 仍 OK、残留仍为 0 就算通过"
    }
  ]
}
