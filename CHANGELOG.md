# 变更记录

本文件是**对外的版本说明**——写给你（正在用、或正在评估这个工具的人）看。每版按四段写，方便你快速判断"要不要升、升了什么、会不会影响到我"：

| 段 | 回答你的问题 |
|---|---|
| **价值** | 为什么值得升级——对**你**有什么不同 |
| **实现** | 新增了什么能力 |
| **修改** | 改了什么、修了什么（含行为变更） |
| **检查** | 凭什么说它是好的——**你可以自己复核**的验证方式 |

> 版本号的唯一权威是仓库的 **tag / Release**，本文件不写"最新版本"字样；按版本倒序（最新在上）。
> 四段里**某一版确实没有内容的段不单列**（例如纯修复版没有"实现"）——**不补写套话**；
> 早期版本按当时的发布记录整理，当时没留下复核方式的写"—"，同样不追补。

## 版本脉络（近三周）

| 版本 | 日期 | 一句话 |
|---|---|---|
| v2.4.17 | 2026-09-19 | 措辞与命名整理（无功能变更）：注释只陈述技术依据；测试脚本改用通用命名 |
| v2.4.15 | 2026-09-19 | 体检把"**要修的问题**"和"**看一眼就行的疑点**"分开：不再每打一条分支就常驻一条红 |
| v2.4.14 | 2026-09-18 | **Windows 从"跑第一条命令就崩"变成正常可用**；上线三平台 CI（含真 Windows），并修掉"声称支持 3.9 但代码用不了 3.9" |
| v2.4.13 | 2026-09-16 | 修"说的和做的不一致"：`--dry-run` 实话化、补机器可读权限字段、加语言切换入口；副作用提示不可静音 |
| v2.4.12 | 2026-09-16 | 按平台安全扫描整改：安装钉 tag、写明权限边界、修文档矛盾、写盘有提示；ClawHub 简介改英文 |
| v2.4.11 | 2026-09-16 | 含**被截断 emoji** 的会话不再打不出分支（以前必失败，且报错内容与"消息里的字"看不出关系） |
| v2.4.10 | 2026-09-15 | **`--session current` 不再可能打到另一个对话**：拿不到"你在哪个对话"的判据就**明确报错**，不猜 |
| v2.4.9 | 2026-09-15 | 修掉默认截断点的一个 14 天潜伏缺陷：**在会话里打分支会把"这一轮"自己也吞进去** |
| v2.4.8 | 2026-09-15 | 从"一个产品的技能"变成**跨 6 个产品的会话分叉工具**；多出的 4 个产品全部过了真机终验 |
| v2.4.7 | 2026-09-14 | 能力边界（产物不一定能回退）从"实现细节"变成**强制执行的行为约束**；对外简介按"给人看"重写 |
| v2.4.6 | 2026-09-14 | 补上最容易被误解的能力边界 + 仓库文档中英双语 |
| v2.4.5 | 2026-09-14 | 目录结构合规重构（修掉平台解析失败）；对外文案按"读者是谁"校正 |
| v2.4.4 | 2026-09-14 | 两条分发渠道的元数据并存；分支校验改三层判定（修误报、补兜底） |
| v2.4.3 | 2026-09-04 | **要么完整生成，要么什么都不留下**（事务化写入） |
| v2.4.2 | 2026-09-04 | 校验前置：失败不留半成品、不污染会话索引 |
| v2.4.1 | 2026-09-03 | 路径越界防护；体检结论分级，不夸大验证等级 |
| v2.4.0 | 2026-09-03 | 从"一次性脚本"变成**可验证的工具**（真库体检成为发布前必检） |

---

## v2.4.17 · 2026-09-19

**价值**

- **措辞与命名整理，无功能变更。** 整理源码注释与文档里的若干表述，使它们只陈述**这个工具本身**的技术依据；测试脚本的名字也换成了更通用的叫法。源码与注释会随公开仓库和安装包一起发布，所以它们读起来应当只关于这个工具。

**修改**

- 6 个文件 18 处：去掉与本项目无关的引用，或换成本身可验证的技术判断（例：`（混用会看错位置，已有反例）`）。**技术内容一字未删。**
- 测试脚本 `regress_fossils.py` → `regress_samples.py`；配套的本地配置模板与忽略规则同步改名。
  ⚠️ **使用既有本地配置的话，需要把顶层键 `fossils` 改成 `samples`**（脚本读 `samples`）；仓库里只保留模板与格式约定，真实样本清单一直在本地、不进仓库。

**检查**

- 双版本测试 **11/11**（Python 3.13.12 / 3.9.6）· 编译 · 产物与源一致性 · 真库体检 通过。

---

## v2.4.15 · 2026-09-19

**价值**

- **体检不再"狼来了"。** 以前只要你打过分支、之后又在分支里写过一次"我的父会话是谁"这类说明，`fork --verify` 就会**永久**给你一条红，还写着"请修复后再发布"——而它根本不是要修的东西。久了没人再看，真问题也就跟着被忽略。这一版把两件事分开：**红 = 要修，黄 = 看一眼就行**。
- **黄档说清"为什么"**：它同时告诉你命中落在哪一类（工具输出 / 思维链 / 消息正文）、追加了多少行、以及**这条分支能否被证实**。想让黄条彻底消失，重打一次该分支即可。
- **新打的分支从此"可自证"**：fork 时会记下前缀指纹。体检拿它核对"前 N 行是否仍是当初复制出来的原样产物"——**边界从假设变成可核对的事实**，不用再靠猜。

**实现**

- 谱系记录新增 `prefix_fp`（前缀指纹：`sha256-1:<摘要>`，带算法标识以便将来换算法）。**6 个产品后端全部落盘**（含 OpenClaw 的两种介质后端）。
- `--verify` 输出新增 `⚠️ 需复核` 档；分项里会写明命中类型（工具 I/O 或思维链 / 正文）与边界证据（指纹一致 / 指纹不符 / 无指纹）。
- **读取路径统一为「容错解析」**：坏行（产品写入过程中被读取时会留下的半行）不再让整轮体检**崩掉**并把其余检查一并跳过，而是**被跳过、并且被点名**（哪几行坏了）。文件型后端一次解析拿到"条目 + 坏行行号"，不重复读。
  （证据等级：本机真库 165 个真实会话**实测 0 例坏行** ⇒ 这是**防御性**加固，不是为了修已发生的事故。）
- 前缀指纹改为**只读前 N 行**（流式），不再为了前几千行去读整份产物（真机最大 107MB）；非文件后端（SQLite 等）自动回退全量读，**正确性优先**。

**修改**

- **判定行为变更（请知悉）**：位于**快照点之后**（fork 完成后产品追加的部分）的源 id 引用，判定从"失败"改为"**需复核**"——不再阻塞、不影响退出码。原因是它与"你在分支里记录血缘"在数据上**无法区分**（两者完全同形）。
  - **曾经试过再收紧、又撤掉了**（本版最纠结的一处，写出来供你判断）：一度加过「若紧接快照点的不是新 user 消息，就从严判失败」，想关掉「污染正好写在快照点正后方」那类形态。**最终没有采用它**，两条理由：① 轴选错了——真实追加里「先开 user 回合」本来就是常态，它挡不住那类形态里最现实的一种（快照点后就是 user 消息、正文里带父 id），那一种照样落在「追加区」；② 支撑它的证据不足——本机只有 WorkBuddy 有 3 条被追加过的真实分支（首行都是 user），其余 5 个后端的追加首行**没有证据**是 user（Claude Code 有压缩摘要条目，Hermes / OpenClaw 有血缘记录条目），一旦它们先写非 user 条目，这条规则会把**合法内容**判成失败，等于重新制造「常驻红」——正是本版要消灭的东西。**现在**：追加区一律只提示复核；但当「紧接快照点的不是新回合」时会在结论里写明这个信号，由你自己看一眼。
  - **代价明说**：同时放弃了"快照点被记小 ⇒ 被跳过的区域里藏着真残留"这一形态的硬拦。兜底有三条：① 创建期整文件零残留硬校验（改写引擎出问题会在落位前就被拒）；② 结构性 `sessionId` / `session_id` 残留检查（完全不看边界）；③ 前缀指纹（能查出"边界之内被改动"）。
  - **关于「别的字段在做会话路由」**：做了一次只读的**真库字段普查**后决定**暂不扩张**硬拦键名。普查脚本随仓库提供（`tools/routing-key-survey.py`，可复跑）：166 个 WorkBuddy 真实会话 / 23 万+ 次出现里，承载会话 id 的键只有 `sessionId`（路由）与 `parentId`/`logicalParentId`（**消息级**链，合法保留）；`conversationId`/`threadId`/`thread_id`/`sessionKey` 出现次数为 **0**。而真实分支里「值含父 id」的键全是 `text`/`value`/`content`/`arguments`/`reasoning` 这类**内容**字段——把它们当路由拦下来，只会让「在分支里写血缘」重新变红（13 条真实分支里 8 条会被新判红）。
  - **同一处的已知缺口（如实标出，没解决）**：这次普查只覆盖 WorkBuddy（本机唯一有真实数据的后端）。别的后端的路由键**不在**硬拦清单里——Codex 是 `payload.thread_id`，pi / OpenClaw 是 `header.id` / `current_session_id`（键名就是 `id`，无法与消息级 `id` 靠键名区分）。后果是**偏漏检、不会误报**：这些后端若在**追加区**出现「路由键＝父 id」，只会得到「需复核」而不是失败；**前缀区**仍由不挑键名的字符串搜索兜住（那才是主拦截层）。要不要升级为硬拦：**先按上面那次普查取证，再改**。
- **仍然硬失败**：前缀（复制区）内残留、结构性 `sessionId` 残留；另外快照点过小/越界时一律退回更宽的边界从严处理（宁可误报也不漏检的那一侧保留在"边界本身不可信"的场合）。
- 汇总项不再把自己算进"需复核"计数（真机实测到 2 项被报成 3 项）。
- 思维链字段 `reasoning` 归入"工具 I/O / 思维链"一类单独计数（此前会被算作"正文"，把正常内容报得更像问题）。
- **坏行按位置分档**（不是"跳过就算"）：落在**快照点之内** ⇒ 该边界不再可信（解析后的条目数与行号已经错位）⇒ 弃用边界并从严；落在**追加区** ⇒ 只提示复核，并写明"可能是产品写入过程中的半行"。
- **源会话里的坏行不再静默**：打分支时会提示"有 N 行无法解析、已跳过、不会被复制进分支"——复制出来的东西少一段，用户有权知道。

**检查**

- 本机真库实测（同一份数据前后对比）：以前是**2 项 ❌ + 退出码 1 + "请修复后再发布"**；现在是**无失败项 / 2 项需复核 / 退出码 0**，并给出命中分类与消解办法。
- 判据矩阵与负向控制（新测试文件，含"真正的问题仍必须被拦住"的反例）：

```bash
python3 tests/test_verify_boundary_semantics.py
```

- 双版本全绿：

```bash
python3 tests/test_wb_adapter.py          # 含分支边界判据（本版改写了其中的期望值）
python3 tests/test_verify_boundary_semantics.py
# 其余测试保持通过：Python 3.13 / 3.9 各 11 个测试文件
```

## v2.4.14 · 2026-09-18

**价值**

- **在这之前，Windows 用户装上它跑第一条命令就会崩**，而且崩得看不懂：中文 Windows 的输出编码（cp936）编不了提示里的 emoji，`print` 直接抛异常；更糟的是**那行报错本身也含 emoji**，于是二次崩——你只看到一行乱码加退出码 1。这一版起正常：**Windows 上中文与 emoji 都能完整输出**。
- **不再有"只在别人机器上才暴露"的缺陷**：仓库此前 **13 个版本全部只在 macOS 上人工测试**，所以"Windows 上跑不起来"这类问题在结构上不可能被发现。本版上线**三平台 CI**（Linux / macOS / **Windows**）——每次改动都会在真 Windows 上跑一遍全部测试。这次的修复本身就是靠它抓出来、并逐轮验证到底的。
- 顺带修掉一处**声明与实现不符**：此前声称支持 Python 3.9，但代码里用了 3.10 才有的写法 ⇒ 3.9 用户一 import 就报错。现在**真的支持 3.9**（已实测）。

**实现**

- 技能说明新增「**平台差异**」一节：macOS/Linux/WSL 与**原生 Windows** 的命令对照表（跑脚本 / 取技能目录 / 家目录 / 环境变量 / 查文件内容 / 设只读），外加四条 Windows 实情——`python3` 在 Windows 上不保证存在（优先用 `py -3`）、`python` 可能被 Microsoft Store 应用别名拦截、PowerShell 执行策略会拦住全局安装的命令行工具、WSL 与原生 Windows 的会话库互不共享。

**修改**

- **输出编码**：命令行入口起手把 stdout 固定为 UTF-8（**仅当当前编码容纳不了中文/emoji 时**；真控制台与已用 UTF-8 的终端不干预）。之所以不用"把编不了的字符替换成 `?`"，是因为在英文 Windows（cp1252）下那样会把**全部中文变成问号**——命令"成功"了，但你读到的是一片 `?`，比直接报错更难察觉。
- **技能声明**：`requires.bins: ["python3"]` → `requires.anyBins: ["python3","python","py"]`。前者的语义是"**每一个都必须存在**"，在 Windows 上会把技能**误判为缺依赖**；后者"至少一个存在"才符合实际。同时把旧的 `metadata.clawdbot` 换成现行的 `metadata.openclaw`。
- **入口拼法**：取技能目录后统一去掉尾斜杠，再拼 `${SK}/scripts/…`——原来的写法依赖取目录命令自带尾斜杠，换一种取法就会拼错路径。
- **提示语补上 Windows 对应**：会话列表刷新提示里原本只写 `⌘Q` / `open -a WorkBuddy`，现在同时给出 Windows 的做法（托盘图标退出后重开）。
- **构建脚本**：Windows 上此前**无法构建**（脚本自身打印的字符在 cp1252 下抛异常）；同时固定产物的换行为 LF（Windows 上不再产出 CRLF 版本，跨平台构建可复现）。
- **Python 3.9 兼容**：8 个模块补 `from __future__ import annotations`（选择保住 3.9，而不是抬高版本下限）。
- 新增 `.gitattributes`（文本一律 LF）与三平台 CI 工作流。

**检查**

- Windows 上的输出（**本版的核心**）：

```powershell
py -3 -m pip install --force-reinstall "git+https://github.com/yamingmou/session-fork-core.git@v2.4.14"
fork --list        # 中文与 emoji 应正常显示；以前这条会以 UnicodeEncodeError 退出
```

- 其他平台的常规自测：

```bash
python3 tests/test_output_encoding.py    # 编码回归（含负例：未加固时必须失败）
python3 scripts/build_skill.py --check   # 技能说明与源一致
```

- 仓库 CI 每次推送都会在 Linux / macOS / Windows 上跑全部测试；其中 Windows 那一路还额外验证「**不设任何编码环境变量**时也能正常输出」。

## v2.4.13 · 2026-09-16

**价值**

- **这一版修的是"说的和做的不一致"**，三处：
  1. **`--dry-run` 的说法自相矛盾过**：文档一处写它"**零写入**"，另一处写它**会写临时文件**——**实现取后者**。照"零写入"理解去用，可能在一个禁止任何写入的环境里踩坑。现在三处统一为实话：**不落位、不登记、不改源会话；会写一个临时文件用于磁盘字节校验，校验完清理**（所以运行处需要写权限）。
  2. **英文读者此前没有入口**：技能说明里**没有任何语言切换链接** ⇒ 英文使用者不知道还有英文版。现在顶部有了（指向英文与中文两版 README）。
  3. **权限声明从"只给人看"变成"也写给机器"**：补上 `allowed-tools` 与 `metadata` 两个**结构化字段**——此前只有正文里的文字说明。
- **副作用动作的提示更可靠**：创建分支文件、写索引、备份源会话、落位、回滚这些动作会打印一行**人类可读**的提示，而且**不会被静默开关关掉**（只有非副作用提示可静音）。

**修改**

- `--dry-run` 的说明三处统一（含 `--help` 文本）。
- 补 `allowed-tools` 与 `metadata` 字段；每个模块头部加一行 `# role:` 文件地图（点明"本文件只是注册表 / 适配器，核心链路在 `engine.py`"）。
- 运行期提示新增"**不可静音**"档位，调用点铺到 备份 / 落位 / 回滚 / 写文件 / 写索引。
- 清掉文档与注释里会误导的权限相关字样（全网只剩一处**真实调用**）。
- 技能说明顶部加语言切换入口。

**检查**

- `--dry-run` 的实际行为与提示（注意 `FORK_QUIET` 的差别）：

```bash
<入口> --session current --dry-run                # 应打印 → 写入分支文件 / writing branch file: … 并给出 Verify : ✅ OK
FORK_QUIET=1 <入口> --session current --dry-run   # 该提示**仍应显示**（副作用提示不可静音）
```

- `--help` 里 `--dry-run` 的说明应与上面一致。
- 8 个测试文件全通过；编译、构建一致性、真库体检、敏感信息扫描全部 0 残留。

---

## v2.4.12 · 2026-09-16

**价值**

- **本版是按一次平台安全扫描的意见做的整改**，四件事都跟你直接相关：
  1. **安装不再"跟着 main 跑"**：安装命令改为**钉住版本 tag**（`…git@vX.Y.Z`）。以前照文档装，今天和明天装到的可能不是一个东西；现在你装到的是**你选定的那一版**。
  2. **说清它到底动你哪些文件**：新增「数据与权限边界」一节——读什么、写哪三处、**明确不做什么**（不联网、不上传、不读凭据、不提权、不装任何东西）。
  3. **修掉文档里一处会误导操作的矛盾**：`--fix` 在本技能与 OpenClaw 官方是**两个不同的东西**（一个是本技能的参数，一个是 `openclaw doctor --fix`），此前两处混用，照文档操作可能在错的平台跑错的命令。现已写明归属。
  4. **有副作用的动作开始"说话"**：创建分支文件、更新索引时，运行期会打印一行人类可读的提示（`→ …`），不再只有代码注释。

**修改**

- 安装 / 升级命令改用**固定 tag**（可复现、可审计；不再从移动分支安装）。
- 新增「数据与权限边界」：读 / 写 / **不做**逐条列明；并标注 `chmod 444` 一律是**给用户的建议**，本技能不会代为执行。
- 澄清 `--fix` 的两个同名不同物（本技能参数 vs `openclaw doctor --fix`）。
- 运行期新增用户可见提示（走 stderr；设 `FORK_QUIET=1` 可静音）。
- **按渠道给不同语言的简介**：ClawHub（受众是 OpenClaw / agent 生态）用英文 `description`；SkillHub 与 WorkBuddy 开放平台保持中文（中文简介保留在 `description_zh`）。
- 顺带修掉文档末尾一处**写死的版本号**（此前停在 2.4.9、两版没跟上）——版本号不再在正文里手写。

**检查**

- 你可以自己复核两件事：
  ` <入口> --session current --dry-run` 应打印 `→ 写入分支文件：…（新建，源会话不改动）` 并给出 `Verify : ✅ OK`；
  `FORK_QUIET=1` 加在同一命令前，该提示应当消失。
- 装到的是哪一版：`<入口> --version`。
- 8 个测试文件全通过；编译、构建一致性、真库体检、敏感信息扫描全部 0 残留。

---

## v2.4.11 · 2026-09-16

**价值**

- **含"被截断的 emoji"的会话，现在也能正常打分支了。**
  先说清以前会发生什么：会话里只要有**一条**消息的 emoji 从中间被截断（常见于从别处粘贴、或中间过了一道会截断文本的工具），
  那么**无论这条会话多长、其余内容多正常**，打分支都会**直接失败**，而报出来的错跟"消息里的字"看不出任何关系：
  `UnicodeEncodeError: 'utf-8' codec can't encode character '\ud83d' ... surrogates not allowed`。
  这个错误**既不说是第几行、也不说哪条消息**——你很难自己定位。所以它值得单独发一版。

**修改**

- 写出分支产物时，若**某一行**含无法直接编码的字符，**只把那一行**改用 JSON 转义形式（`\uXXXX`）写出；
  **其余行一字不动**。转义在 JSON 层是**等价**的：读回来仍是原来那个字符——**不替换、不丢弃、不改写内容**。
  实测（10 万行级会话）：**仅 1 行**被转义，其余**逐字符相同**。
- 覆盖**全部 6 个产品后端**（WorkBuddy / Claude Code / Codex / pi / OpenClaw 的两种存储后端 / Hermes），
  含**文件写出**、**数据库写入**与**索引写出**三条路径——不是只修其中一个产品。

**检查**

- 你可以自己复核（最直接）：对以前会失败的那条会话先预演，应正常完成并打印 `Verify : ✅ OK`：
      <入口> --session <id> --dry-run
- 想确认产物没被"改内容"：把分支里与源里**同一条消息**的文本比对，应当**逐字相同**。
- 本版新增一组针对性用例：含该字符的**真实样本**、孤高/孤低**合成样本**、**合法 emoji 必须不被改写**（防"修好一处、弄坏正常 emoji"）、
  以及**旧的写法必须仍然报错**（证明测到的是真问题，而不是空转）。
- 8 个测试文件全通过；构建一致性、真库体检、敏感信息与写死路径扫描全部 0 残留。

---

## v2.4.10 · 2026-09-15

**价值**

- **打分支不会再打到"别人的对话"**：`--session current` 现在只有两种结果——解析出**你正在其中的那个对话**，或者**明确报错**并告诉你该改用什么。**不再有第三种**：悄悄给你换一个看起来像的。
- **源会话名当场回显**：输出新增 `Source : <id> 「<会话名>」`。此前只给 36 位十六进制 id，**人根本看不出对不对**；现在打完分支扫一眼名字就知道有没有打错。
- **旧行为没有删，只是不再冒充 `current`**：确实想从"最近活跃的那个会话"打，用 `--session latest-working`——名字终于说真话了。

**实现**

- `--session current` 解析改为**硬失败**：执行环境里拿不到会话标识 ⇒ 报错退出，**不回退、不猜**。
- 新增 `--session latest-working`：显式表达"库里最近一个 `status='working'` 的会话"（即旧版 `current` 的真实语义）。
- 输出回显源会话名（取 `custom_title`，退化到 `title`）。

**修改**

- **修复 `--session current` 静默打到另一个对话**（v2.4.5 起一直存在，2026-09-15 定位）。真实事故：同一个会话内 8 分钟两次**完全同形态**的命令，一次父会话是**我正待在的那个对话**（对），一次是**另一个对话**（错）——而两次输出都只给 36 位 id，**人看不出差别**。分版本说清：
  | 版本 | `current` 的实际行为 |
  |---|---|
  | ≤ v2.4.6 | 一律取"库里最近一个 `status='working'` 的会话" |
  | v2.4.7 / v2.4.8 | 同上——**完全不看执行环境标识**，并行开几个会话时**必错** |
  | v2.4.9 | 优先用执行环境标识；**标识缺失或过期时仍回退到同一条 SQL** ⇒ 本次事故就是这条支路 |
  | **v2.4.10** | 缺判据就**报错**，不猜 |
- **订正 v2.4.9 的变更说明**：v2.4.9 那条里写着"标识过期则回退存储，不拿它去撞'找不到'"——**这句话把缺陷写成了特性**，本次事故正是它描述的那个回退造成的。该说法自本版起不再成立。
- **锚点预览不再打印系统注入块**：`--dry-run` 里"锚定 Lxxx 的 user 消息「…」"这段，此前取的是消息开头的字面量，而你发的消息**首部常被系统塞入上下文块** ⇒ 打印出来是 `<system-reminder data-role="…`，**你照样核对不出是不是自己刚说的那句**。现在跳过这些包裹块，取真正的人话（人话在第二块里时也认）。
- **`--session latest-working` 补进 `--help`**，且不再被"这不像个 ID"的提示误伤（该提示的白名单原先只认 `current`）。
- **修掉文档里一个不存在的入口路径**：生成的 WorkBuddy 版文档把长命令入口写死成 `~/.workbuddy/skills/session-fork/scripts/create_branch.py`，而实际安装目录可能带渠道后缀（如 `session-fork__skillhub/`）⇒ **照抄必然报"找不到文件"**。改为运行期取路径。

**检查**

- **你可以自己复核**（这一条最关键）：在任意目录跑
  `env -u CLAUDE_SESSION_ID -u CODEBUDDY_SESSION_ID -u BAGGAGE python3 scripts/create_branch.py --session current --dry-run`
  ⇒ 应**报错并退出码 1**，列出三条可用替代。旧版会**静默挑一个会话继续**——那就是事故现场。
- 新增 8 条针对 `current` 解析的用例（含"存在更新的 working 会话时不得改主意"）；
  原有一条**把该回退断言成期望行为**的用例**已反转**为"必须硬失败"。
- 8 个测试文件全通过（7 个适配器 + 本版新增的解析用例）；
  构建一致性、文档事实核验（含"fork 版命令必须带 `--adapter`"）、敏感信息与写死路径扫描**全部 0 残留**。

---

## v2.4.9 · 2026-09-15

**价值**

- **分支真的从"上一轮结束"处岔开了**：你在对话里说"打分支"，新分支拿到的是**上一轮为止**的全部上下文——"打分支"这条指令本身、以及执行它时产生的所有过程（试跑、检查输出、脚本自己的回显）都**不再跟着进新分支**。
- **切在哪，你能当场核对**：`--dry-run` 会打印走了哪种判定、锚在**第几行**、以及**那一行原文是什么**（例如 `锚定 L14169 的 user 消息「Please continue with…」`）。此前输出里只有 `line 18782 / 18836` 一个数字，无从核对——真实事故里执行者就是因此"以为切对了"。
- **"整份复制"依然可用**：需要把当前内容连本轮一起带走时，`--whole` 一次性覆盖。

**实现**

- **默认截断点分成两种语义，由执行环境自动判定**（不再让"一种规则套所有场景"）：
  - **会话内打分支**（脚本跑在被 fork 的那个会话里）→ 切在**上一轮输出结束**：最后一条 user 消息之前的最后一条完整 assistant 回复。
  - **从外部执行**（终端 / 定时任务 / 显式指定别家会话）→ **整份复制**到末尾最后一条完整 assistant 回复。
- 适配器接口新增 **`running_session_id()`**：报出"执行本脚本的进程处在哪个会话里"。默认实现读 Claude Code / WorkBuddy 系留在执行环境里的会话标识；无此信号的产品返回 `None`，**行为与旧版一致**，需要时自行覆盖。
- 新增 **`--whole`**：在会话内强制"整份复制"。

**修改**

- **修复默认截断点的 14 天潜伏缺陷**（v2.1.0 → v2.4.8 一直存在，2026-09-15 定位）：
  在会话里打分支时，切点落在**本轮自己的叙述**上。真实两例（同一台机器同一天，现场复原）：
  | 分支 | 实际切点 | 正确切点 | 多吞 |
  |---|---|---|---|
  | `3ccebc53`（源 `ec48e1ae`） | L18782 | L18763 | **19 行**（含"打分支"指令本身） |
  | `5df71252`（源 `d6af6ce6`） | L8452 | L8424 | **28 行** |
  切点还会随执行者继续说话而前移：同一轮内 `--dry-run` 报 18778、正式执行写 18782（漂移 4 行），而"切在最后一条 assistant 文本"这件事**永远看着像对的**。
  根因是提交 `82df3a5`（v2.1.0，2026-09-01）：为支持"分支再 fork 时整份复制"，把原实现里"锚在最后一条 user 消息之前"的 4 行删掉、只留全文件倒扫——**主用例被一起改坏**，且同一提交把文档也改成了相符的错误描述（"默认模式找的是最后一条 assistant 回复（文件末尾）"），此后一直自洽地错。本版把两种语义拆成两个函数分派，文档同步订正。
- **`--session current` 的解析改用执行环境标识**：`current` 的本义是"我正在其中的那个会话"，原先靠数据库里"最近一个 `status='working'` 的会话"猜——并行会话 / 新开会话都会让它指错。现在优先用执行环境标识（且校验该会话确有 transcript，标识过期则回退存储，不拿它去撞"找不到"）。
  - **[修改 2026-09-15] 上一句里"标识过期则回退存储"是错的**——那个回退**正是本版引入的缺陷**，会在标识拿不到时静默打出**另一个对话**的分支（2026-09-15 真实事故）。已由 **v2.4.10** 修复为"硬失败"。此处按只加不减的纪律保留原文并追加订正，不就地改写。

**检查**

- 两个真实事故现场用修复后的引擎复跑，切点落到正确行（且 `--dry-run` 的锚点原文与人工核算一致）。
- 6 个适配器测试全通过（`workbuddy` / `claude_code` / `codex` / `hermes` / `pi` / `openclaw` ×2 后端），其中 `workbuddy` 新增一组用例把**两个事故的形状**钉成回归：整份语义 → L7（吞本轮）／会话内语义 → L2（正确），以及退化情形（无 user 消息、单轮会话）明确退回整份。
- **基线回归（真实会话样本）**：用修复前的代码采基线、修复后再跑，**4 条全部一致**——证明"显式指定会话"这条路径**逐字节零漂移**，行为变更只发生在会话内默认分支上。
- 你可以自己复核：在任意会话里跑 `--dry-run`，核对打印出的锚点行号与行原文是不是"你说打分支"的那条消息。

---

## v2.4.8 · 2026-09-15

**价值**

- **不再被单一产品绑住**：同一个"打分支"能力现在覆盖 **WorkBuddy · Claude Code · Codex · Hermes · pi · OpenClaw**（OpenClaw 两个世代都支持）。你在哪个产品里工作，就在哪个产品里打分支。
- **你能知道自己在拿哪种分叉语义**：我们把"引用式 vs 复制式"这条分界用实测钉死了——Codex 的原生分叉是**引用式**（只存指针，省空间但依赖源存活），本项目是**复制式**（自包含，源被删也照样能续跑）。这不是宣传话术，是两边都跑过之后的对照结论。
- **失败会自己告诉你正确写法**：把"一段文字"误当成会话 ID 时，工具会直接给出该用的命令，而不是抛一句"找不到"。
- **产物可被产品自己接管**：分支生成后，由产品自己列出、自己续跑、自己维护索引——不依赖我们伪造任何"已校验"标志。

**实现**

- 新增 5 个适配器：`adapter_claude_code` · `adapter_codex` · `adapter_hermes` · `adapter_pi` · `adapter_openclaw`（+ `adapter_openclaw_sqlite`，按介质自动选后端）。
- 引擎抽象出**行存储与转录引用**：转录可以不是文本文件（例如 SQLite 的 `(session_id, seq) → event_json`），只要静息态能表达为**有序 JSON 条目序列**。
- 适配器接口扩展：`list_all_sessions` / `sort_key` / `read_raw` / `is_historical_id_path`——让不同产品的"会话枚举、最近使用排序、原始文本读取、历史字段豁免"各有实现。
- **版本闸**：读产品的 schema 版本，**不等于已知值即拒绝写入**并提示升级适配器，而不是静默写坏你的会话库。
- **备份跟着产品走**：备份目录不再只有 WorkBuddy 一套——Claude Code / Codex / Hermes / pi / OpenClaw 各备份进**自己产品的 home**（如 `~/.claude/fork-backups`），不互相污染；想统一指定用环境变量 `FORK_BACKUP_DIR`。

**修改**

- 修掉引擎里 **3 处"转录 = 文件"的隐含假设**（排序键取文件 mtime、备份范围说明、只读提示），改为由适配器提供。
- 修掉"**文件名 = 会话 id**"的假设（pi 的文件名是 `<时间戳>_<id>.jsonl`）。
- 删掉基类里**重复的 `read_lines` 定义**：接口桩与默认实现同名同签名、被静默覆盖，会让人误以为"改桩即改行为"。
- CLI：支持**包外直接执行**（`python3 fork_core/cli.py …` 与 `python3 -m fork_core.cli …` 等价）；新增**参数形状体检**；两处"未命中"报错补对症提示——**形状体检负责"早提醒"，错误路径负责"全兜底"**。
- 文档：命令写法**全文归一**（此前文档里同时存在三种入口写法，会让 AI 照抄出不存在的命令）；`SKILL.md` 瘦身（更新历史移出到本文件）；`README` 安装节改为**按"你在用哪个产品"组织**，非 WorkBuddy 用户有独立路径。
- `SKILL.md` 改为**产品感知**（此前它默认"你在 WorkBuddy 里"）：
  - 新增 **Step 0 · 先判定你在哪个产品**——命令入口三选一（WorkBuddy 完整路径 / pip 的 `fork` / 其他产品的技能目录）+ 判定顺序；
  - 原先被当成"通用事实"的 WorkBuddy 专属内容逐条标注：界面上的「复制请求 ID」按钮、重启客户端才能在列表看到、`--fix`（仅 workbuddy）、日志写 `.workbuddy/memory/`、会话存储路径；
  - 「查看新分支」不再写死"重启 WorkBuddy"，改为**照脚本输出的 `ACTION : 分支已创建——…` 转述**（那句话本就按产品适配：Claude Code 用 `/resume`、pi 无需重启、OpenClaw 可能需重启 Gateway）；
  - 故障排查表增加「**适用**」列，把"仅 WorkBuddy"与"全部"分开，并补上非 WorkBuddy 的对应处置。

**检查**

- **真机终验覆盖 4 个产品**：Claude Code / Codex / Hermes / OpenClaw（两个世代）。判据不是"文件生成了"，而是**产品自己列出这条分支、并能在这条分支上继续对话**——Hermes 的续跑里，模型答出了只存在于被复制前缀里的信息（说明上下文真的接上了）；OpenClaw 续跑后由产品自己把索引维护到最新。
- **你可以自己复核**：`python3 tests/test_*.py`（7 个测试文件，当前全绿）、`python3 -m fork_core.cli --verify`（真库体检，当前全绿）。
- 新增**真实会话回归**：把真实会话中的关键行为（截断点、校验结论）固定成只读基线，改代码后逐条比对，漂移即失败——用于防止"悄悄退化"。
- **已知边界（如实说）**：参数形状体检对"单个 ≥8 字符的纯英文单词"不告警（它看起来就像 ID）；这类输入会在后续查表阶段被明确提示，不会静默失败。

---

## v2.4.7 · 2026-09-14

- **价值**：把一条最容易踩的边界**变成每次都会履行的行为**——"会话可随意分叉，**产物不一定**"。此前这条只存在于实现里，用户很容易把"分叉"理解成"时光倒流"，然后在"回到旧产物"这个真实诉求上被坑。
- **实现**：`SKILL.md` 新增「分叉边界」章——四类产物能否回到切点的判定表（git 已提交 ✅ ／有版本记录的载体 ✅ ／无版本记录的本地文件 ❌ ／已发生的外部副作用 ❌），并写成 4 条强制规则：创建前先交代边界、动手前先探工作目录 git 状态、"回到旧产物"要先打检查点、快照不得说成"丢数据"。
- **修改**：技能简介（`description`）从"给机器读的路由契约"改回"给人看"——它同时是模型判断该不该用的依据、也是平台商品页的门面，此前平台上展示的简介读起来像实现文档。
- **检查**：真实会话上实际打一次分支（不只看预览），边界与产物判定与文档一致；发布前 5 项检查全过。

## v2.4.6 · 2026-09-14

- **价值**：补上一条**最容易被误解的能力边界**，并把仓库文档补齐**中英双语**（`README.md` 中文默认展示 + `README.en.md` 英文，顶部互切）。
- **实现**：`SKILL.md` 新增「分叉边界」章（判定表 + 4 条给 AI 的执行规则）。
- **修改**：仓库文档双语对齐，边界一节两种语言同权重呈现。
- **检查**：—（本版为文档与行为约束改动，未新增可独立复核的检查项）
- **发布说明**：本批改动在仓库侧**以一次提交承载**（与 v2.4.7 不拆成两次提交），SkillHub 商品页展示为 v2.4.6。

## v2.4.5 · 2026-09-14

- **价值**：修掉上传平台会**直接解析失败**的目录层级问题（平台要求技能包最多两级目录）。
- **修改**：`fork_core/` 从 `scripts/` 提到根目录；适配器拉平为同级模块；`tests/` 提到根目录。同时修掉两个测试里硬编码的本机绝对路径（改为按 `__file__` 自定位，**任何机器可直接跑**），并移出版本控制里的构建残留。
- **检查**：目录深度最深 1 层、违规 0（`git ls-files | awk -F/ 'NF-1>1'` 可自行复核）；打包后在平台上可被正常解析；`pip install` 后从任意目录执行入口正常。

## v2.4.4 · 2026-09-14

- **价值**：修掉一类**误报**——分支被正常追加消息后，校验不再把它误判成"源 id 残留"（真实案例：追加的工具输出里恰好有个目录名等于源会话 ID）。
- **实现**：分支校验改为三层判定：① fork 前缀按谱系快照点划界、任意字段从严；② 全文件结构性残留硬查（不依赖边界）；③ 快照点之后的追加区**只对工具/函数 I/O 字段宽容**（那里出现源 id 属对话内容）。
- **修改**：补开放平台元数据字段（`display_name` / 双语 description），一份包同时喂两个渠道；修复测试污染生产谱系索引（测试未隔离索引路径）。
- **检查**：真库级（L2）全绿；负向验证——即使快照点记录偏小，"被跳过那段"里的正文残留仍会被抓住。

## v2.4.3 · 2026-09-04

- **价值**：**要么完整生成，要么什么都不留下**。
- **实现**：`tmp` 原子写 + 落位前校验 + 异常分层；`--dry-run` 走**完整**校验路径（预览的结论对真实执行有效）。
- **检查**：校验失败时 db / 文件 / 谱系三者全干净。

## v2.4.2 · 2026-09-04

- **价值**：**不留半成品**。
- **修改**：校验前置到登记之前——失败时既没有半成品文件，也不会污染会话索引。
- **检查**：顺序焊死（校验失败 ⇒ 登记零调用、临时文件零残留）。

## v2.4.1 · 2026-09-03

- **价值**：安全与诚实性修正。
- **修改**：`--session` 路径越界防护（拒绝分隔符 + realpath 容器校验）；体检结论分级（L1 fixture / L2 真库 / L3 产品终验），不夸大验证等级。
- **检查**：越界路径被拒绝；体检结论按级别如实标注。

## v2.4.0 · 2026-09-03

- **价值**：把"分叉"从一次性脚本变成**可验证的工具**。
- **实现**：递归 id 替换 + 原始内容黑名单；`--verify` 真库体检成为发布前必检。
- **检查**：真库级体检成为每次发布的固定关卡。
